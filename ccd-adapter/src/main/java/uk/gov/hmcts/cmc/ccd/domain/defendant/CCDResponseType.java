package uk.gov.hmcts.cmc.ccd.domain.defendant;

//NOTE: Enum names need to be identical to enum names in ResponseType
public enum CCDResponseType {
    FULL_DEFENCE,
    FULL_ADMISSION,
    PART_ADMISSION
}
