#!groovy

@Library("Infrastructure@feature/add-slack-integration")

def product = "cmc"
def component = "claim-store"

withPipeline(type: "java", product: product, component: component, slackChannel: '#cmc-tech-notification') {
  after('checkout') {
    echo 'claim-store checked out'
  }
}
